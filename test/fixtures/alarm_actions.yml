precip_alarm_websocket:
  alarm: precip_3h_belem
  trigger_state: alarm
  action_type: notification
  configuration:
    channels:
      - websocket

precip_ok_websocket:
  alarm: precip_3h_belem
  trigger_state: ok
  action_type: notification
  configuration:
    channels:
      - websocket
