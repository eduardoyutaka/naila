<% content_for(:title, "Naila — Novo Usuário") %>

<div class="mx-auto max-w-2xl">
  <div class="mb-6">
    <h1 class="text-xl font-semibold text-naila-text">Novo Usuário</h1>
  </div>

  <%= form_with model: [:admin, @user] do |f| %>
    <%= render "form", form: f, user: @user %>
  <% end %>
</div>
