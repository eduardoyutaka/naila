<% content_for(:title, "Naila — Editar Estação") %>

<div class="mx-auto max-w-2xl">
  <div class="mb-6">
    <h1 class="text-xl font-semibold text-white">Editar Estação</h1>
  </div>

  <%= form_with model: [:admin, @monitoring_station] do |f| %>
    <%= render "form", form: f, monitoring_station: @monitoring_station %>
  <% end %>
</div>
