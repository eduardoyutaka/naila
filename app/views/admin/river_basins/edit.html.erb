<% content_for(:title, "Naila — Editar Bacia Hidrográfica") %>

<div class="mx-auto max-w-3xl">
  <div class="mb-6">
    <h1 class="text-xl font-semibold text-white">Editar Bacia Hidrográfica</h1>
  </div>

  <%= form_with model: [:admin, @river_basin] do |f| %>
    <%= render "form", form: f, river_basin: @river_basin %>
  <% end %>
</div>
